# API Configuration
API_URL=http://localhost:3000/api

# Production API URL (optional, defaults to API_URL if not set)
# API_URL_PROD=https://api.dynamap.run.place/api

# Environment
PRODUCTION=false
